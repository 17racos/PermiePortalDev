<!DOCTYPE html>
<html lang="en">
  <head>
    <title>PermiePortal | Your Hub for Permaculture Knowledge</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", skip_pipeline: true %>


    <!-- Meta Tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="PermiePortal: Your central resource for permaculture education and community networking.">
    

    <!-- Stylesheets -->
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Include JavaScript with Import Maps -->
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-green-50 font-sans text-gray-800">
    <div class="min-h-screen flex flex-col">

      <!-- Header Section -->
      <header class="bg-gradient-to-r from-green-600 to-green-500 text-white shadow-lg">
        <div class="max-w-screen-lg mx-auto flex items-center justify-between py-4 px-4 sm:px-6 lg:px-8">
          <!-- Logo -->
          <h1 class="text-2xl font-extrabold tracking-wide">
            <%= link_to 'PermiePortal', root_path, class: "hover:text-green-200 focus:text-green-300" %>
          </h1>

          <!-- Navigation -->
          <nav class="flex space-x-4 text-sm font-semibold">
            <% if user_signed_in? %>
              <span>Welcome, <%= current_user.email %>!</span>
              <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'hover:text-green-200 focus:text-green-300' %>
            <% else %>
              <%= link_to 'Login', new_user_session_path, class: 'hover:text-green-200 focus:text-green-300' %>
              <%= link_to 'Sign Up', new_user_registration_path, class: 'hover:text-green-200 focus:text-green-300 ml-4' %>
            <% end %>
          </nav>
        </div>
      </header>

      <!-- Main Content -->
      <main class="flex-grow max-w-screen-lg mx-auto py-8 px-6 sm:px-8 lg:px-10">
        <div class="grid grid-cols-1 lg:grid-cols-4 gap-12 min-h-screen">
          <!-- Yield to content in individual views -->
          <%= yield %>
        </div>
      </main>

      <!-- Footer -->
      <footer class="bg-gray-800 text-white py-6">
        <div class="max-w-screen-lg mx-auto text-center">
          <p class="text-gray-400 text-sm">
            &copy; <%= Time.current.year %> PermiePortal. All rights reserved.
          </p>
          <p class="text-gray-400 mt-2">
            Explore more at:
            <a href="https://permaculture.org" target="_blank" class="text-green-400 hover:text-green-300 focus:underline">Permaculture.org</a>
          </p>
        </div>
      </footer>

    </div>
  </body>
</html>
